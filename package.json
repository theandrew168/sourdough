{
	"name": "webgl",
	"private": true,
	"version": "0.0.0",
	"type": "module",
	"scripts": {
		"dev": "npm run build -- --serve=127.0.0.1:5000 --servedir=public",
		"build": "esbuild frontend/main.js --outfile=public/main.js --bundle --minify",
		"format": "prettier --write frontend/"
	},
	"devDependencies": {
		"esbuild": "^0.15.2",
		"prettier": "^2.6.2"
	},
	"dependencies": {
		"gl-matrix": "^3.4.3",
		"webgl-obj-loader": "^2.0.8"
	},
	"prettier": {
		"useTabs": true,
		"semi": true,
		"singleQuote": true,
		"bracketSpacing": true,
		"trailingComma": "all",
		"printWidth": 100
	}
}
